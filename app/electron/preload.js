// No privileged APIs are exposed to the renderer — the app runs exactly
// like it does in a browser (localStorage-backed), just inside a native
// window. Nothing here needs Node or Electron access.
